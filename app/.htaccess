<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /ITE311-ESYONG/

    # If the request is not a file or directory, send it to index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>
